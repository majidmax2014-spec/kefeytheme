query RunInput {
  cart {
    lines {
      id
      quantity
      bundleDiscount: attribute(key: "_kefey_bundle_discount") {
        value
      }
      bundleDiscountLabel: attribute(key: "_kefey_bundle_discount_label") {
        value
      }
      upsellDiscount: attribute(key: "_kefey_upsell_discount") {
        value
      }
      upsellDiscountLabel: attribute(key: "_kefey_upsell_discount_label") {
        value
      }
      merchandise {
        __typename
        ... on ProductVariant {
          id
        }
      }
    }
  }
}
